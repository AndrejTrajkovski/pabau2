import XCTest
@testable import AddBookout

final class AddBookoutTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(AddBookout().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample)
    ]
}
